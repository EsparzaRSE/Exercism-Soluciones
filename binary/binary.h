#pragma once

#ifndef BINARY_H
#define BINARY_H

#include <string_view>
#include <string>
#include <cmath>

namespace binary {
    int convert(std::string_view str_binario);
}  // namespace binary

#endif // BINARY_H